<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #121212;
            color: #ffffff;
        }

        .navbar {
            background: linear-gradient(90deg, #ff8c00, #e52e71);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        .card {
            background: #1e1e1e;
            color: white;
            transition: transform 0.3s, box-shadow 0.3s;
            border-radius: 10px;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.2);
        }

        .btn-primary {
            background: linear-gradient(90deg, #ff8c00, #e52e71);
            border: none;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Movie Management</a>
            <div class="ms-auto">
                <a href="/add" class="btn btn-primary">Add New Movie</a>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h2 class="text-center mb-4">Movie List</h2>
        <div class="row">
            <% allMovies.forEach((movie) => { %>
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <% if (movie.poster) { %>
                            <img src="<%= movie.poster %>" class="card-img-top" alt="<%= movie.title %>" style="height: 300px; object-fit: cover;">
                        <% } %>
                        <div class="card-body">
                            <h5 class="card-title"><%= movie.title %></h5>
                            <p class="card-text"><strong>Director:</strong> <%= movie.director %></p>
                            <p class="card-text"><strong>Genre:</strong> <%= movie.genre %></p>
                            <p class="card-text"><strong>Year:</strong> <%= movie.releaseYear %></p>
                            <p class="card-text"><strong>Rating:</strong> ⭐ <%= movie.rating %>/10</p>
                            <p class="card-text"><%= movie.description.substring(0, 100) %>...</p>
                            <div class="d-flex justify-content-between">
                                <a href="/edit/<%= movie.id %>" class="btn btn-warning btn-sm">Edit</a>
                                <a href="/delete/<%= movie.id %>" onclick="return confirm('Are you sure?')" class="btn btn-danger btn-sm">Delete</a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html> -->


